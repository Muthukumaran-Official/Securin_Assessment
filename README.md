# Procedure
1. Required Installations : 
   `pip install fastapi uvicorn sqlite3 requests pandas`
2. Run the api.py file using : 
   `uvicorn api:app --reload`
3. Click on the local host link generated to access the Frontend as well as Backend.
